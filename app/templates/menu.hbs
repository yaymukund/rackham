<nav id="menu">
  <ul>
    {{#if session.user}}
      <li class="logged-in">Logged in as <span class="login">{{session.user.login}}</span></li>
    {{/if}}

    <li class="home">{{#link-to 'index'}}Home{{/link-to}}</li>
    <li class="feedback"><a {{action 'openFeedback'}} href="#">Give feedback!</a></li>

    {{#if session.user}}
      <li class="logout"><a {{action 'logout'}} href="#">Logout</a></li>
    {{else}}
      <li class="login">{{#link-to 'login'}}Login{{/link-to}}</li>
      <li class="register">{{#link-to 'login'}}Register{{/link-to}}</li>
    {{/if}}
  </ul>
</nav>
